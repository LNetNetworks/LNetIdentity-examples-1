# Base path of the Wallet API. The OpenAPI document declares `/wallet` as the
# server base, so it is part of the URL. Optional — this is also the built-in
# default in src/api/client.ts.
VITE_IDENTITY_API_BASE_URL=https://dev-identity-dwallet.l-net.io/wallet
